diff --git a/build.gradle b/build.gradle
index 2ca8ec60e..79be72b39 100644
--- a/build.gradle
+++ b/build.gradle
@@ -179,6 +179,9 @@ android {
         testApplicationId 'org.futo.inputmethod.latin.tests'
         testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
         vectorDrawables.useSupportLibrary = false
+        ndk {
+             abiFilters "arm64-v8a"
+        }
     }
 
     bundle {
@@ -366,6 +369,17 @@ android {
         mlModelBinding true
         buildConfig true
     }
+    Calendar origin = Calendar.getInstance()
+    origin.set(2021, Calendar.DECEMBER, 31, 23, 59)
+    Date d1 = origin.getTime()
+    Date d2 = new Date()
+    long minutes = (d2.getTime() - d1.getTime()) / 1000 / 60
+    def DateVersionCode = 2112312359 + minutes
+    android.applicationVariants.all { variant ->
+        variant.outputs.all { output ->
+            output.versionCodeOverride = DateVersionCode
+        }
+    }
 }
 
 dependencies {
@@ -434,4 +448,4 @@ dependencies {
 }
 
 project.logger.lifecycle("versionCode = ${android.defaultConfig.versionCode}")
-project.logger.lifecycle("versionName = ${android.defaultConfig.versionName}")
\ No newline at end of file
+project.logger.lifecycle("versionName = ${android.defaultConfig.versionName}")
