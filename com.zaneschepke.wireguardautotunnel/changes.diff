diff --git a/app/build.gradle.kts b/app/build.gradle.kts
index 74d2f60f..1bf4f857 100644
--- a/app/build.gradle.kts
+++ b/app/build.gradle.kts
@@ -1,5 +1,7 @@
 import com.android.build.api.dsl.ApplicationExtension
 import com.android.build.api.variant.FilterConfiguration
+import java.time.LocalDateTime
+import java.time.Duration;
 
 plugins {
     alias(libs.plugins.android.application)
@@ -41,8 +43,8 @@ configure<ApplicationExtension> {
             val noSplits = providers.gradleProperty("noSplits").isPresent
             isEnable = !noSplits
             reset()
-            include("armeabi-v7a", "arm64-v8a")
-            isUniversalApk = !noSplits
+            include("arm64-v8a")
+            isUniversalApk = false
         }
     }
 
@@ -93,7 +95,6 @@ configure<ApplicationExtension> {
                 getDefaultProguardFile("proguard-android-optimize.txt"),
                 "proguard-rules.pro",
             )
-            signingConfig = signingConfigs.getByName(Constants.RELEASE)
             resValue("string", "provider", "\"${Constants.APP_NAME}.provider\"")
         }
 
@@ -138,6 +139,17 @@ configure<ApplicationExtension> {
         resValues = true
     }
     packaging { resources { excludes += "/META-INF/{AL2.0,LGPL2.1}" } }
+	val d1 = LocalDateTime.of(2021, 12, 31, 23, 59);
+	val d2 = LocalDateTime.now()
+	val minutes = Duration.between(d1, d2).getSeconds() / 60
+	val DateVersionCode = (2112312359 + minutes).toInt()
+	androidComponents {
+		onVariants { variant ->
+			variant.outputs.forEach { output ->
+				output.versionCode.set(DateVersionCode)
+			}
+		}
+	}
 }
 
 androidComponents {
